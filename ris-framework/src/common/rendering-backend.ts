/**
 * The rendering backend enum.
 */
export enum RenderingBackend {
    /**
     * The WebGL 2 is used as the rendering backend.
     */
    WEB_GL = "webgl",

    /**
     * The WebGPU is used as the rendering backend.
     */
    WEB_GPU = "webgpu"
}